"""Gap chats: by-interview reconciliation of legacy-doc memories with the code.

`service.open_gap_chat` raises one question when a merge makes a legacy memory
look stale; `service.submit_answer` applies the engineer's answer, upgrading the
memory to verified or superseding it. See service.py for the full flow.
"""
